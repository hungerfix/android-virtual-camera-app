      - name: Build Debug APK
        run: |
          set +e

          gradle assembleDebug --stacktrace 2>&1 | tee build.log
          EXIT_CODE=${PIPESTATUS[0]}

          echo ""
          echo "========================================"
          echo "BUILD ERROR SUMMARY"
          echo "========================================"

          grep -A 15 -B 5 \
            -E "FAILURE:|What went wrong:|Execution failed for task|Could not resolve|e: |error:" \
            build.log || true

          echo "========================================"

          exit $EXIT_CODE
